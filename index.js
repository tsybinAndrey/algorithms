const {binarySearch} = require('./binarySearch')
const {gcd} = require('./gcd')

const array = [1, 3, 5, 7]
const stringArray = ['a', 'b', 'c', 'd']

console.log(binarySearch(stringArray, 'e'))
console.log(gcd(1680, 640))